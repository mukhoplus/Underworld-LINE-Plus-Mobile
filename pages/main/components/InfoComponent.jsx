import React from 'react';
import {Text} from 'react-native';

const InfoComponent = ({
  userId,
  setUserId,
  userList,
  setUserList,
  roomList,
  setRoomList,
  setRoomId,
  setChatList,
  allNotReadCount,
}) => {
  return <Text>정1보</Text>;
};

export default InfoComponent;
