import React from 'react';
import {Text} from 'react-native';

const ChatComponent = ({
  userId,
  roomList,
  setRoomList,
  roomId,
  setRoomId,
  chatList,
  setChatList,
}) => {
  return <Text>채팅방</Text>;
};

export default ChatComponent;
